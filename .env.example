# Audio device names or ALSA hw strings
# Run: python -m app.main --list-devices  to find your device names/indices
SOUND_CARD_EN=hw:1,0
SOUND_CARD_ZH=hw:2,0
SAMPLE_RATE=16000
AUDIO_CHANNELS=1

# STT (faster-whisper)
# Models: tiny, base, small, medium, large-v3
# larger = more accurate, slower
WHISPER_MODEL=base
WHISPER_DEVICE=cuda
WHISPER_COMPUTE_TYPE=float16

# Translation model cache dir
TRANSLATION_MODELS_DIR=./models/translation

# TTS (piper-tts)
# Download voices via scripts/download_models.sh
PIPER_VOICE_EN=en_US-lessac-medium
PIPER_VOICE_ZH=zh_CN-huayan-medium
PIPER_MODELS_DIR=./models/piper

# Push-to-talk keys (keyboard mode)
KEY_RECORD_EN=1
KEY_RECORD_ZH=2
